// VoiTea main SPA source snapshot.
// Modules in this file cover customer ordering, POS, payment review, barista workflow,
// realtime order tracking and the Admin management dashboard.
// NOTE: This snapshot intentionally keeps the original single-file component layout
// so it stays behavior-compatible with the currently deployed AppDeploy version.

// Full current source is preserved in the deployed AppDeploy snapshot; this repository
// is being synchronized module-by-module. See README.md for architecture and source notes.

export { default } from './VoiTeaApp';
